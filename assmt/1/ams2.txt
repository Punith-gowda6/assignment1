step 1 : start
step 2 : Declare sum,temp,tnum,rem
step 3 : get the armstrong number
step 4 : initilize sum=0 and temp = num
step 5 : for (;i<=999;i++)
	tnum = i;
	sum = 0;
	while (tnum > 0)
		rem = tnum % 10
		temp = rem * rem * rem
		sum = temp + sum
		tnum = tnum / 10
	if i=sum 
		print sum
print 6 : stop